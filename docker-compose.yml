version: "3.9"
services:
  mongo:
    image: mongo:7
    ports: ["27017:27017"]
    volumes: ["mongo_data:/data/db"]

  web:
    build: ./apps/web
    env_file: ./apps/web/.env.local
    ports: ["3000:3000"]
    depends_on: [mongo]

volumes:
  mongo_data:
